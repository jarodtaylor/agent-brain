// eval/cases.jsonl — one JSON object per line (lines starting with // are ignored).
// Author 5–10 cases AFTER freezing the promoted corpus (U6 execution note).
// Schema: { "query": "...", "expect_slug": "...", "expect_source_episode": "..." }
