# dependencies / build
node_modules/
dist/
build/
*.tsbuildinfo

# env / secrets — never commit real values
.env
.env.*
!.env.example

# local-only notes (internal, never meant for the public repo)
*.local.md
*.local.*

# local workflow tooling (skills, session config) — kept local, per the agent-os convention
.claude/

# os / editor
.DS_Store
*.log
.idea/
.vscode/
